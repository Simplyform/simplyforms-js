<!-- Thanks for contributing to the SimplyForms client! -->

## What & why

<!-- What does this change do, and why is it needed? Link any related issue. -->

Closes #

## Checklist

- [ ] Tests added/updated and `pnpm -r test` is green
- [ ] `pnpm lint` and `pnpm -r typecheck` pass
- [ ] `pnpm -r check:exports` passes (publint + are-the-types-wrong)
- [ ] Added a changeset (`pnpm changeset`) if this changes published behavior
- [ ] The SDK stays dependency-free at runtime (no new runtime `dependencies`)
- [ ] No real secret values, tokens, or form IDs appear in code, tests, or screenshots

## Notes for reviewers

<!-- Anything that needs special attention, trade-offs, or follow-ups. -->
